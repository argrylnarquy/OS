all:
  children:
    servers:
      children:
        webservers:
          hosts:
            debian:
              ansible_user: vagrant
              ansible_password: vagrant
              ansible_ssh_private_key_file: ~/.ssh/ansible_key
        dnsservers:
          hosts:
            fedora:
              ansible_user: vagrant
              ansible_password: vagrant
              ansible_ssh_private_key_file: ~/.ssh/ansible_key
